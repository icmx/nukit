export const ERROR_MUST_HAVE_GET_KEY_FUNCTION = new TypeError(
  'getKey function is required for byKeys objects sorting'
);

export const ERROR_MUST_BE_NON_EMPTY_ARRAY = new TypeError(
  'order must be a non-empty array'
);
