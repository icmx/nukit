/**
 * (Pseudo) Random Numbers Generator.
 *
 * Must be a function that returns a float number between inclusively
 * 0 and exclusively 1, like standard Math.random.
 */
export type RNG = () => number;
